### Whatagraph dev test exercise by Dmitrijs Čornobiļs
## install
1. run `composer install`
1. copy/use credentials from .env.example below and paste in .env
   
## usage
1. run `php artisan app:weather`
1. run tests with `vendor/bin/phpunit`
